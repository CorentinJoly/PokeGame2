<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}PokéGame{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('images/favicon.ico') }}" />
        <link rel="icon" type="image/x-icon" href="{{ asset('images/favicon.ico') }}" />
        <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
        <script src='https://www.google.com/recaptcha/api.js'></script>
        <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,700' rel='stylesheet' type='text/css'>
    </head>

    <header>
        <div class="navigation">
            <span class="navigation__logo"><a href="{{ path('homepage') }}">PokéGame</a></span>
                <nav class="navigation__menu">
                    <!-- Il faut laisser cette ligne sinon ça fou la merde, je sais pas pk -->
                    <a></a>
                    {% if app.user %}
                    <ul style="list-style: none;">
                        <li><a href="{{ path('new') }}">Nouvelle capture</a></li>
                        <li><a href="{{ path('annonces') }}">Annonces</a></li>
                        <li><a href="">Déconnexion</a></li>
                    </ul>
                    {% endif %}
                </nav>
        </div>
        <div class="separateur"></div>
    </header>
    
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
